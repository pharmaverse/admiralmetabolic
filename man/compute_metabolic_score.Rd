% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_metabolic_score.R
\name{compute_metabolic_score}
\alias{compute_metabolic_score}
\title{Compute Metabolic Scores}
\usage{
compute_metabolic_score(
  mode,
  alt = NA_real_,
  ast = NA_real_,
  bmi = NA_real_,
  t2dm = NA_character_,
  sex = NA_character_,
  triglycerides = NA_real_,
  ggt = NA_real_,
  waist_circumference = NA_real_,
  age = NA_real_,
  platelets = NA_real_,
  albumin = NA_real_,
  insulin = NA_real_,
  glucose = NA_real_
)
}
\arguments{
\item{mode}{A character string specifying the metabolic score to compute.
Possible values are "HSI", "FLI", "NAFLDFS", "HOMAIR".}

\item{alt}{Alanine Aminotransferase (ALT) value (U/L).
A numeric vector is expected. Required for "HSI" and "NAFLDFS" modes.}

\item{ast}{Aspartate Aminotransferase (AST) value (U/L).
A numeric vector is expected. Required for "HSI" and "NAFLDFS" modes.}

\item{bmi}{Body Mass Index (BMI) value (kg/m2).
A numeric vector is expected. Required for "HSI", "FLI", and "NAFLDFS" modes.}

\item{t2dm}{Type 2 Diabetes Mellitus (T2DM) status ("Y" or "N").
A character vector is expected. Required for "HSI" and "NAFLDFS" modes.}

\item{sex}{Sex ("M" or "F").
A character vector is expected. Required for "HSI" mode.}

\item{triglycerides}{Triglycerides value (mg/dL).
A numeric vector is expected. Required for "FLI" mode.}

\item{ggt}{Gamma Glutamyl Transferase (GGT) value (U/L).
A numeric vector is expected. Required for "FLI" mode.}

\item{waist_circumference}{Waist Circumference value (cm).
A numeric vector is expected. Required for "FLI" mode.}

\item{age}{Age value (years).
A numeric vector is expected. Required for "NAFLDFS" mode.}

\item{platelets}{Platelets value (x 10^9/L).
A numeric vector is expected. Required for "NAFLDFS" mode.}

\item{albumin}{Albumin value (g/dL).
A numeric vector is expected. Required for "NAFLDFS" mode.}

\item{insulin}{Fasting plasma insulin value (mIU/L).
A numeric vector is expected. Required for "HOMA-IR" mode.}

\item{glucose}{Fasting plasma glucose value (mmol/L).
A numeric vector is expected. Required for "HOMA-IR" mode.}
}
\value{
A numeric vector of computed metabolic scores.
}
\description{
This function computes various metabolic scores based on the specified mode.
}
\examples{
# Compute HSI
compute_metabolic_score(
  mode = "HSI",
  alt = c(30, 45), ast = c(25, 40), bmi = c(28, 32), t2dm = c("N", "Y"), sex = c("M", "F")
)

# Compute FLI
compute_metabolic_score(
  mode = "FLI",
  triglycerides = c(150, 200), bmi = c(28, 32), ggt = c(40, 55), waist_circumference = c(90, 105)
)

# Compute NAFLDFS
compute_metabolic_score(
  mode = "NAFLDFS",
  age = c(50, 65), bmi = c(28, 32), t2dm = c("N", "Y"),
  ast = c(30, 45), alt = c(25, 40), platelets = c(200, 150), albumin = c(4, 3.5)
)

# Compute HOMA-IR
compute_metabolic_score(
  mode = "HOMAIR",
  insulin = c(10, 15), glucose = c(5, 6)
)
}
